[package]
name = "r-boot"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
log = "0.4"
uefi = { version = "0.39", features = [
    "alloc",
    "global_allocator",
    "logger",
    "panic_handler",
    "log-debugcon",
] }
